# SSH target, as it would be passed to `ssh`.
HOST=deploy@203.0.113.10
# The root Caddy serves from. Must exist and be owned by the SSH user.
DEST=/var/www/dither
# URL prefix. `/` when the app is served on its own subdomain.
PUBLIC_URL=/dither/
